# Incoming (not lake-built)

`TailRecurrence.lean` is a complementary authoring of Sun eq. 1.4 (Catalan tail recurrence on `ℝ`). Status as received: CAPABILITY-LIMITED, two `sorry`s (`summable_tailTerm`, `tail_pos`).

Do not import this module from `CatalanSun.lean` until those sorries are gone and `lean-proof-forge` still reports `pass`.
